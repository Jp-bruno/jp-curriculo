import styled from 'styled-components';

const StyledTransition = styled.div`
    height: 100vh;
    background: black;
`;

const TransitionPage = () => <StyledTransition />;

export default TransitionPage
